name: 'environment resources workflow'
#test
on:
  push:
    branches:
      - dev
      - main

jobs:
   terraform:
    name: 'terraform'
    if: contains(github.event.head_commit.message, 'build-iac')
    runs-on: ubuntu-latest

    #    defaults:
    #      run:
    #        shell: bash
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checkout
        uses: actions/checkout@v2

      - name: if main, terraform init, validate, plan, apply
        run: |
                echo "iac creation using terrafrom"
   build_image:
    name: 'test ci'
    if: contains(github.event.head_commit.message, 'biuild-image')
    runs-on: ubuntu-latest

    #    defaults:
    #      run:
    #        shell: bash
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Checkout
        uses: actions/checkout@v2

      - name: if main, terraform init, validate, plan, apply
        run: |
                echo "docker image creation"
